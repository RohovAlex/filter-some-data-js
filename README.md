# filter-some-data-js

This function filters data in the table. We have filter menu pop-up with 8 check boxes, which are responsible for 8 types of documents.
Using these check boxes we can show documents and parts of these documents(nested documents) with selected types.
